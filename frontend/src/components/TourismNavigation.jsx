import React from 'react';
import { Box, Typography } from '@mui/material';

function TourismNavigation() {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
      }}
    >
      <Typography variant="h4">Tourism Navigation</Typography>
    </Box>
  );
}

export default TourismNavigation;
